# Install CUDA-enabled PyTorch after the base package install.
# PyPI only hosts the CPU-only torch build; CUDA wheels live on PyTorch's own index.
#
# Usage:
#   pip install -e .                        # or: pip install lightningnbeats
#   pip install -r requirements-cuda.txt   # upgrades torch to CUDA 12.8 build
#
# For other CUDA versions see: https://pytorch.org/get-started/locally/
#   CUDA 12.1: --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8: --index-url https://download.pytorch.org/whl/cu118
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.1.0

